@model Microservice.Web.ViewModels.Bilgi.BilgiDetailVm
@{
    ViewData["Title"] = Model.Baslik ?? "Bilgi";
}

<div class="page-title parallax parallax1">
    <div class="container">
        <div class="flat-university">
            <div class="post">
                <h1 class="title">@(Model.Baslik ?? "Bilgi")</h1>
            </div><!--/post -->
        </div><!--/flat-university -->
    </div><!--/container -->
</div><!--/page-title -->

<section class="flat-row">
    <div class="container">
        @if (!string.IsNullOrWhiteSpace(Model.IcerikMetni))
        {
            <div>@Html.Raw(Model.IcerikMetni)</div>
        }
        else
        {
            <p>Bu bilgi için içerik girilmemiş.</p>
        }
    </div>
</section>
