@model Microservice.Web.ViewModels.Haber.HaberDetailVm
@{
    ViewData["Title"] = Model.Baslik ?? "Haber";
}

<div class="page-title parallax parallax1">
    <div class="container">
        <div class="flat-university">
            <div class="post">
                <h1 class="title">@(Model.Baslik ?? "Haber")</h1>
            </div><!--/post -->
        </div><!--/flat-university -->
    </div><!--/container -->
</div><!--/page-title -->

<section class="flat-row">
    <div class="container">
        @if (!string.IsNullOrWhiteSpace(Model.IcerikMetni))
        {
            <div>@Html.Raw(Model.IcerikMetni)</div>
        }
        else
        {
            <p>Bu haber için içerik girilmemiş.</p>
        }
    </div>
</section>