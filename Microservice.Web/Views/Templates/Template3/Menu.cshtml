@model Microservice.Web.ViewModels.Template.TemplatePageViewModel
@{
    ViewData["Title"] = Model.CurrentMenu?.Ad ?? "Menü";
}

<main class="container py-5">
    <p class="text-uppercase text-muted small">Template 3</p>
    <h1>@(Model.CurrentMenu?.Ad ?? "Menü")</h1>
    <div class="mt-4 fs-5">
        @Html.Raw(Model.CurrentMenu?.Icerik ?? "Bu menü için içerik girilmemiş.")
    </div>
</main>
